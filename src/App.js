import React from 'react';
import { Box, Card, Typography } from '@material-ui/core';

import { Clock, Cards } from './components';

const App = () => {
  return (
    <div>
      <Clock />
      <Cards />
    </div>
  );
};

export default App;
